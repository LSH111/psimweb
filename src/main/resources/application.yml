spring:
  profiles:
    #active: dev
  config:
    activate:
      on-profile: default
  datasource:
    # PostgreSQL을 기본 설정으로 변경
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:ksi4788500!}
    hikari:
      # PostgreSQL 기본 설정
      maximum-pool-size: ${DB_MAX_POOL_SIZE:10}
      minimum-idle: ${DB_MIN_IDLE:5}
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

      # 연결 검증
      connection-test-query: "SELECT 1"
      validation-timeout: 3000

      # PostgreSQL 최적화 설정
      data-source-properties:
        stringtype: unspecified
        ApplicationName: psim-web

# MyBatis 설정
mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: com.psim.web.*.vo
  configuration:
    map-underscore-to-camel-case: true
    call-setters-on-nulls: true
    jdbc-type-for-null: NULL
    cache-enabled: true
    lazy-loading-enabled: false
    aggressive-lazy-loading: false
    default-statement-timeout: 30
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl  # 🔥 SQL 로깅 활성화

# 로깅 설정
logging:
  level:
    root: INFO
    com.psim: DEBUG
    com.psim.web: DEBUG
    com.psim.web.prk.mapper: TRACE     # 🔥 주차장 Mapper SQL 로깅
    com.psim.web.cmm.mapper: TRACE     # 🔥 공통 Mapper SQL 로깅
    com.zaxxer.hikari: INFO
    org.springframework.jdbc: DEBUG
    org.springframework.transaction: DEBUG
    org.mybatis: DEBUG
    org.apache.ibatis: TRACE            # 🔥 추가
    java.sql: TRACE                     # 🔥 추가
    java.sql.Connection: DEBUG          # 🔥 추가
    java.sql.Statement: DEBUG           # 🔥 추가
    java.sql.PreparedStatement: DEBUG   # 🔥 추가
    java.sql.ResultSet: TRACE           # 🔥 추가
  pattern:
    console: |

      %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{40})
      %msg%n
    file: |

      %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{40}
      %msg%n
  file:
    name: logs/psim-web.log           # 🔥 로그 파일 저장
    max-size: 10MB
    max-history: 30

# 서버 설정
server:
  port: ${PORT:8080}
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
    session:
      timeout: 30m  # 30분 세션 타임아웃
      cookie:
        secure: false  # 🔥 개발 환경에서는 false (HTTP 허용)
        http-only: true  # JavaScript 접근 차단
        same-site: lax  # 🔥 strict → lax로 변경 (로컬 개발 편의성)

management:
  endpoints:
    web:
      base-path: /api  # 이 설정이 있으면 /api/health로 매핑됨
      exposure:
        include: health
  endpoint:
    health:
      show-details: when-authorized